# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Genevieve Romanelli
description: >- # this means to ignore newlines until "baseurl:"
  Genevieve Romanelli | PhD student in mathematics at CUNY Graduate Center.
  Studying smooth math in rough spaces (currently, characterizing properties of energy forms on graphs).
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://giromanelli.github.io" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings

sass:
  sass_dir: _sass
  style: compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

footer: 'email me at <a href="mailto:gromanelli&#64;gradcenter.cuny&#46;edu">gromanelli&#64;gradcenter.cuny&#46;edu</a>'

# Theme-specific settings
header_pages:
  - index.markdown
  - CV.markdown

style: light # dark (default), light, nord, or hacker
listen_for_clients_preferred_style: true # false (default) or true
disable_google_fonts: false

defaults:
  - scope:
      path: ""
    values:
      layout: default

include:
  - assets/js
  - assets/pdfs
  - assets/images


# tracking:
#   script_src:
#     - https://www.googletagmanager.com/gtag/js?id=G-SEKMK3QF2W
#   async: true   # default true
#   defer: false  # default false
#   # Optional inline init snippet (requires CSP allowance if used)
#   init: |
#     window.dataLayer = window.dataLayer || [];
#     function gtag(){dataLayer.push(arguments);}
#     gtag('js', new Date());
#     gtag('config', 'G-SEKMK3QF2W');

# Extend CSP to allow your tracker sources (script-src/connect-src/img-src as needed)
# csp_extra: "script-src 'self' https://www.googletagmanager.com 'unsafe-inline'; connect-src 'self' https://www.googletagmanager.com; img-src 'self' https://www.googletagmanager.com;"


# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
